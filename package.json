{
  "name": "aurora",
  "version": "1.0.0",
  "description": "Documentation for The Aurora Platform",
  "license": "Apache-2.0",
  "scripts": {
    "start": "cd gatsby-starter-skatteetaten && npm start",
    "prebuild": "npm run clean",
    "build": "cd gatsby-starter-skatteetaten && npm run build",
    "deploy": "gh-pages -d public -p 0",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf public gatsby-starter-skatteetaten/.cache"
  },
  "devDependencies": {
    "gh-pages": "2.1.1",
    "husky": "3.1.0",
    "prettier": "1.19.1",
    "pretty-quick": "2.0.1",
    "rimraf": "3.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
